# StockSummaryApi

A minimal .NET 8 API project for stock summary information.

## Prerequisites

- .NET 8 SDK

## How to Run

Run the following command from the project directory:

```bash
dotnet run
```

## Base URL

The API will be available at:

- HTTPS: `https://localhost:7148`
- HTTP: `http://localhost:5274`
